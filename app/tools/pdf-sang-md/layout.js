export const metadata = {
  title: "Chuyển PDF sang Markdown — PDF Việt",
  description: "Chuyển file PDF sang định dạng Markdown để dùng với ChatGPT, Claude, Gemini. Xử lý ngay trên trình duyệt, không upload server.",
};

export default function Layout({ children }) {
  return children;
}
